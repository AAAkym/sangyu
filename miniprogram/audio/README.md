# 语音播报音频目录

放置 TTS 预录音频（MVP 用预录文件，后续可换云端 TTS 实时合成）：

- `alert.mp3` —— 内容「您有新的求助，请及时查看」（家属端收到新告警时播放）
- `sos_sent.mp3` —— 内容「求助已发送，家属将尽快响应」（老人端 SOS 发送成功后播放）

说明：`utils/voice.js` 按 text 匹配音频文件；文件缺失或播放失败时自动降级为 3 秒文字 toast，不阻塞告警流程。
