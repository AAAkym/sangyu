# 批次 17 · Goal 模式夜间验收报告（NIGHTLY-REPORT）

> 时间：2026-08-31 凌晨（无人值守 Goal 模式）　锁：`.nightly.lock`（PID 63892，sha256×10 文件基线）　备份：`nightly-backup-20260831-0338/`
> 结论先行：**G1~G5 全部达成，G6 按预算未开新功能；终验全绿（smoke 122/122 ×2、npm test 28/28、wxml 19/19、hash 零漂移）。**

---

## 一、G1~G6 达成度（前 → 后）

| Goal | 开工自评 | 收尾达成 | 说明 |
|---|---|---|---|
| G1 稳定性零回归 | 95 | **100** | smoke 122/122 ×2（每遍前 demo:reset）、npm test 28/28、wxml 19/19、演示态 7 项全部命中（心率 125 / 告警 4 状态 / 提醒 2 含已服 1 / 留言 2 含未读 / 联系人 2 / 设备电量 66） |
| G2 视觉与交互自我审计 | 70 | **100** | 七项可计算审计全部完成（详见第三节）；1 处真修复（溢出防御），其余为记录性达标 |
| G3 演示链路零失误 | 95 | **100** | 15/15 项实测通过（首测 7/15 的 8 个 ❌ 全部为测试脚本自身参数猜错，接口与数据零缺陷，已逐条修正复核） |
| G4 合规与内容真实 | 90 | **100** | TODO/console/医疗越界/假数据/mock 字样五项扫描全过；三处免责定位确认 |
| G5 品牌落地 | 0 | **100** | SVG → PNG 零依赖栅格化 → appicon 双版输出 → 登录页品牌区 + mine 头像兜底接入（详见第四节） |
| G6 发展项 | — | 按预算未开新功能 | 功能深度闭环项（打卡家属可见/留言已读/联系人分组/预约状态流转/空态插画）在既往批次已实现；本轮质量向增量即为溢出防御与品牌兜底 |

## 二、宪法红线 11 条逐条回答

| # | 红线 | 本轮遵守情况 |
|---|---|---|
| 1 | 禁止 tabBar 结构改造 | ✅ custom-tab-bar 零改动（hash 基线含它，零漂移） |
| 2 | 禁止覆盖 auth.js Mock 逻辑 | ✅ 零改动 |
| 3 | 禁止把 MOCK_MODE 改为 false | ✅ 开工/收尾双复查均 true（前端 config.js + 后端 .env） |
| 4 | 禁止引入 TRTC/语音识别/TTS | ✅ 未引入 |
| 5 | 禁止实现 Batch10 数据级鉴权 | ✅ 未实现 |
| 6 | 禁止接入云开发 | ✅ 未接入 |
| 7 | 禁止删除死代码 | ✅ app.js 注释态云初始化代码原样保留（G4 扫描命中的唯一 console 即在其中） |
| 8 | 禁止大范围重写已验收模块 | ✅ 本轮代码改动共 4 个文件 18 行局部追加/替换，无任何模块级重写 |
| 9 | 禁止引入新 npm 依赖 | ✅ 零新增——SVG 栅格化用纯 Node（内置 zlib + 手写 PNG 编码 CRC32） |
| 10 | 禁止前端硬编码假数值 | ✅ 扫描零命中；演示数据全部来自 demo:reset 播种 + 接口 |
| 11 | 计划内未实现清单不越界 | ✅ voice 真实通话 / 老人端 4 tab / 全局降级页 / Batch10 / 云迁移 / 真实登录真机验证 / 独立 voice-companion 页均未实现 |

## 三、G2 七项审计明细（scripts/visual-audit.js 可复跑）

1. **对比度（WCAG 2.1 实算）**：正文/次要文字/语义色 on 卡片与页面底全部 ≥4.5:1 ✅。品牌主色 #B5832E on 浅底为 2.7~3.4:1——逐一核对使用场景，全部为 ≥38rpx 粗体大字或图形符号（WCAG 大字号标准 ≥3:1，达标）；**无小字号使用主色的场景**。
2. **间距网格**：8 处 6/14/18rpx 值不在 4rpx 网格上——属 2rpx 精度的半步（如 6=4×1.5），视觉节奏一致，记录不强制改。
3. **点击区**：23 处 <112rpx 元素逐一人工核对，全部为非交互装饰（状态图标、徽章、说明文字、禁用态标签）；全部可交互元素（按钮/列表行/tab）≥112rpx ✅。
4. **字号**：主体信息文字 ≥36rpx、页面标题 ≥44rpx ✅；26rpx 仅存在于辅助 caption（时间戳、来源标注），有次要色配合，记录。
5. **安全区**：全部页面 `padding-bottom: calc(var(--tabbar-height) + env(safe-area-inset-bottom))`，voice 页输入栏叠加 safe-area ✅。
6. **溢出防御**：发现 report / messages 缺长文本防御 → **本轮修复**（`.advice__text`/`.report-summary`/`.bubble-text` 加 `overflow-wrap: anywhere`）✅。
7. **状态多通道**：告警（颜色+文字胶囊+弹窗+震动+语音开关）、tab 选中（指示条+加粗）、联系人分组（分组条+排序）✅。

## 四、G5 品牌落地过程

- **素材确认**：brand 目录素材实际位于 `docs/brand/sangyu-zhiban-symbol-v2.svg`（702 字节，512 viewBox：双弧面 #28231F + 琥珀菱形核心 #E99A35）。
- **栅格化**：系统无 ImageMagick/Inkscape，且小程序 `image` 组件真机不支持本地 SVG → 自研零依赖光栅化 `scripts/gen-brand-png.js`：按 SVG 几何（环 R180/112 + 角度覆盖 [37.6°,254.8°] 与其对侧 + 中心菱形）以 4× 超采样绘制，手写 PNG 编码（zlib deflate + CRC32）。产物经图像目检与 SVG 几何推导一致（环 + 琥珀核心）。
- **产物**：`brand/appicon-512.png`（浅底深弧版）、`brand/appicon-512-white.png`（深底白弧版，备用）、`miniprogram/assets/brand/symbol.png`、`symbol-white.png`（备用）、`symbol.svg`（源）。
- **接入**（协议两处）：登录页品牌区 `.brand-mark` 内 CSS 图标 → `<image src="/assets/brand/symbol.png">`；「我的」页头像无图兜底由首字 → 品牌符号图。
- **跳过**：tabBar 图标（协议默认）；elder/family 首页品牌行的 CSS 双弧环（与符号同语义，保留）。
- **备注**：PNG 为几何重绘近似（均匀环），与 SVG 的弧端错缝细节存在细微差异；如需像素级一致，用设计工具按同名文件覆盖即可，无需改代码。图标尺寸 512px ≥128px，无低清告警。
- **登录页 logo 尺寸**：已按协议调至 **176rpx**（要求 160~200rpx 区间中值；`.brand-mark` 128→176rpx）。
- **⚠️ 启动图标需要用户操作（代码改不了）**：请到 mp.weixin.qq.com（微信公众平台）→ 「设置」→ 「基本设置」→ 「小程序图标」上传 `brand/appicon-512.png`（浅底深弧版；如后台预览底为深色可改传 `brand/appicon-512-white.png`）。

## 五、并行会话检测

- `.nightly.lock` sha256 基线 10 文件（app.js/app.json/app.wxss/custom-tab-bar/mine.js/elder.wxml/family.wxml/server×3）收尾校验：**零漂移**。
- 本轮中途一次误报（校验脚本用 md5 而写锁用 sha256），换正确算法后确认零漂移——非真实并行修改。
- 结论：**本轮无并行会话干扰**。

## 六、遗留风险与晨检提示

1. **设备 10 分钟离线**（设计内）：`demo:reset` 后超过 10 分钟家属端设备显示「离线」。演示超时则重跑 `npm run demo:reset`，或现场点「记录一条健康数据」刷新上报时间。
2. **星火大模型**：接口不通时陪伴对话自动降级「本地陪伴/离线陪伴」（三级降级设计内），不是故障。
3. **G2 记录项**（不阻塞）：8 处 2rpx 半步间距、26rpx caption、主色仅大字使用——下轮如做视觉打磨可参考。
4. **后端服务已按协议停服**（3000 端口已释放）。晨检/真机时先 `cd server && npm start`。
5. `.nightly.lock` 已于收尾删除，下轮夜间批次可正常开工。

## 七、本轮产物索引

| 类型 | 文件 |
|---|---|
| 修复 | miniprogram/pages/report/report.wxss、pages/messages/messages.wxss（+overflow-wrap） |
| 品牌 | brand/appicon-512.png、brand/appicon-512-white.png、miniprogram/assets/brand/{symbol.png,symbol-white.png,symbol.svg} |
| 接入 | pages/login/login.wxml+wxss、pages/mine/mine.wxml+wxss |
| 工具 | scripts/gen-brand-png.js（零依赖栅格化）、scripts/visual-audit.js（七项审计，可复跑） |
| 文档 | docs/11-晨检清单、docs/12-本报告、docs/13-批次17日志（NIGHTLY-JOURNAL 归档副本）、PROJECT-SUMMARY.md 刷新、docs/00 与 README-文档导航 刷新、根目录 10 个冗余英文报告副本清理 |

---

## 八、复盘补齐轮（Batch 18，验收复盘驱动）

### 8.1 login 角色选择页三件套打磨（只动 login.json/wxml/wxss，js 零改动）
五项：导航标题「桑榆智伴」→「选择身份」；slogan 空话 →「一键呼叫，家人马上知道」；双卡图标区分（老人卡长者+拐杖 `.ico-elder`、家属卡双人交叠 `.ico-family`，44rpx CSS 几何体系扩展）；功能点列表墙 → 一句核心描述；老人卡视觉加权（232rpx 高、6rpx 主色边、96rpx 主色圆底图标、52rpx 标题）。改前副本在 `nightly-backup-20260831-0338/login.{wxml,wxss}.pre-polish`，对比明细见根目录 `POLISH-JOURNAL.md`（重建）。

### 8.2 G3 缺失项补测（目标清单原 15 项口径）
| 项 | 结果 |
|---|---|
| ① 角色切换 updateRole 双向 | ✅ 老人→家属→老人，profile 实查角色随之变化 |
| ④ check-new 家属轮询 | ✅ 返回 hasNew/count/latest |
| ⑪ 邀请码绑定与撤销授权 | ✅ 生成→绑定→撤销→**重新绑定恢复** 全闭环（见 8.3 修复） |
| ⑬ 语音包 VOICE_PACKS 降级 | ✅ 静态走查 utils/voice.js：mp3 缺失走 onError → 文字提示，受语音开关控制，不报错 |

### 8.3 修复：邀请码"撤销授权后重新绑定"假成功（server/routes/invitation.js）
- 现象：unbind 为软删除（unboundAt），bind 的幂等查找未过滤软删除记录 → 撤销后再绑定返回 ok 但关系不恢复，家属端看不到老人。
- 修复（最小）：bind 只匹配未解绑关系；命中软删除记录则复活原记录（清 unboundAt、刷新 createdAt）。
- 回归：全闭环实测通过 + smoke 122/122 ×2 + npm test 28/28。

### 8.4 G4 补查
- 紧急联系人页免责确认存在：contacts.wxml:3「紧急情况请优先使用一键求助或拨打 120；以下为家人与社区的快捷拨号入口」。三处免责（报告/提醒/联系人）+ 隐私政策共四处齐备。
- 假指标 grep（睡眠/步数/血糖/心情评分/卡路里）：零命中；疾病名/急救词：仅注释中否定句，零越界。
