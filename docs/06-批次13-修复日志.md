# FIX-JOURNAL · 修复与打磨日志（Batch 13）

- 开始：2026-08-30（P0→P1→P2 顺序执行）
- 备份：`fix-backup-20260830-1343/`（miniprogram + server，排除 node_modules）
- 基线：MOCK_MODE 双 true ✓；3000 端口空闲 ✓；smoke 基线 117 项 117/117
- emoji 残留摸底：family(📞⚠️🔔📱📡👥🩺📈🌻+8 个 nth-child 图标)、elder(💬❤️📅👥⏰💌🆘🤝📞🌻🔊)、mine/login(🧑‍🦳👨‍👩‍👧👤💛)、voice(🌻🎤)、reminders(⏰)、report(📈)、privacy — 待 P2 清零

---

## P0-1 修复角色切换（演示级阻断 Bug）

- **现象**：老人端无法退出登录、无法切到家属端。
- **根因**：老人端 custom-tab-bar 仅渲染 1 个 tab（首页），物理上无入口进入 mine 页；且 mine 是 tabBar 页，普通页面无法 navigateTo 跳转（微信限制）。
- **修法**：elder 首页 Hero 右上角加「设置」入口（wx.switchTab 跳 mine——mine 是 tabBar 页，switchTab 是唯一可行方式且不触碰 tabBar 结构）；mine 页双角色化（身份卡/关系列表/切换身份/退出登录均按角色适配）。
- **文件**：（实施后补记）
- **验证结果**：（待验）
- **状态**：进行中

---

## P0-1 修复角色切换 ✅ 通过

- **根因**：老人端 tabBar 仅 1 个 tab，无 mine 入口；且 mine 是 tabBar 页，navigateTo 无法跳转（微信限制）。
- **修改**：pages/elder/elder.wxml（Hero 右上角三横线设置入口）+ elder.js（onSettingsTap → wx.switchTab 跳 mine，switchTab 是 tabBar 页唯一可行跳转方式且不改 tabBar 结构）+ pages/mine/mine.js（setSelected 按角色适配：elder→0/family→1；新增 onSwitchRoleTap 双向切换身份；身份标题 identityTitle 按角色生成）。
- **验证**：node --check ✓；代码路径复核（elder 设置 → switchTab mine → 退出清 role+停轮询 → login 重选 ✅ 双向）；smoke 两遍 122/122。
- **状态**：通过。

## P0-2 修复健康页误弹 toast ✅ 通过

- **根因**：演示工具按钮（写入 Mock/生成 7 天数据）平铺在页面底部，答辩演示时易误触造成数据突变与 toast 打扰。
- **修改**：pages/health/health.js（+showTools/toggleTools）+ health.wxml（按钮收进「演示工具（联调用）」折叠区，默认收起、显式展开才可触发，并加说明文案）。健康页数据加载本身走 silent 请求，无自动 toast。
- **验证**：代码路径复核（onLoad/onShow 无任何 showToast）✅；老人端进入 health-elder 直接显示大数字 ✅。
- **状态**：通过。

## P1-mine 个人信息页（双角色统一）✅ 通过

- **修改**：pages/mine/mine.js + .wxml + .wxss。个人信息区（头像取昵称首字、身份标题：老人=昵称/家属=「XX的家属」、监护关系摘要）；适老化设置（字体三档复用 + **高对比模式开关**（Storage contrastMode → font.js 附 .hc-mode → app.wxss 变量组整体提对比）+ **语音播报开关**（Storage voiceSwitch → utils/voice.js speak() 单点控制，关闭后告警弹窗/震动不受影响））；监护与隐私（关系列表 + 二次确认撤销 + 隐私入口）；关于（版本 v0.9.0-demo、数据来源、**演示模式标识**）；切换身份入口（P0-1）。
- **验证**：所有设置持久化 Storage 并即时生效（mine 页 setData + 重启后 font.js/fontClass 读取）；smoke 两遍 122/122。
- **状态**：通过。

## P1-chat 语音陪伴打字聊天（三级降级）✅ 通过

- **实现**：server/routes/chat.js + utils/chatEngine.js（9 组适老陪伴语料，组内随机、兜底轮换、记忆称呼）+ utils/sparkClient.js（讯飞 Spark Lite OpenAI 兼容端点；Bearer 仅用 SPARK_API_PASSWORD；信号量 max 2 在途 + 550ms 节流 + 30s 排队超时；流控 11200~11203 识别为可降级）；chats.json 留痕。
- **降级链**：配置 SPARK_API_PASSWORD → spark；未配置/超时/5xx/流控/解析失败 → 本地 chatEngine；未命中 → 兜底反问。医疗红线：不适话题一律引导联系家人或就医（chatEngine 单测 9 项锁定，含越界词黑名单）。
- **前端**：pages/voice 重写为聊天界面（scroll-view 消息列表、输入框 112rpx、发送禁用连点、打字机 40ms×2 字、自动滚动、首次引导气泡、source 可视化「由讯飞星火生成/本地陪伴模式」、无 emoji）。
- **验证**：npm test 28/28（validators 11 + ruleEngine 8 + chatEngine 9）；smoke 两遍 122/122（chat 问候/红线/空消息/降级 source）；当前 .env 密钥为占位 → 实测 source=local 降级链 ✅。
- **状态**：通过。

## P2 前端去 AI 味（适老化安全区内）✅ 通过

- **修改**：app.wxss（+ico 纯 CSS 图标系统 10 种形状、+缓动变量 --ease-out/--ease-spring、+.fade-up stagger、.entry 去描边改分层、page-title 负字距+行高 1.25）；emoji 全站清零 32 处（family 15、elder 11、login 3、mine 2、contacts 1）替换为统一几何图标（ico-person/chart/pulse/phone/alert/clock/box/lines/wave/ring）；family 主入口强化（健康数据 featured + 文案「看今天的心率/最新一条 {{panelHint}}」真实摘要）；间距全部走 --space 网格；中性色沿用暖调令牌。
- **禁用手法确认**：未打破对称、未加噪点/玻璃拟态/装饰渐变、未降对比度、未缩字号。
- **验证**：emoji grep 清零 ✓；WXSS 无新增硬编码色值 ✓；smoke 两遍 122/122 ✓；适老化复核见下。
- **状态**：通过。

## 适老化硬指标复核（改造后）

- 主文字 ≥32rpx（36rpx）✅；特大档 ≥44rpx ✅；行距标题 1.25/正文 1.7 ✅
- 主按钮 ≥112rpx（11 页）✅；次级工具按钮为设计权衡（记录）⚠️
- 对比度 ≥4.5:1（三级文字提色后）✅；状态不只靠颜色（胶囊+文字+图标）✅
- 适老模式开关：字体大小 + 高对比模式（mine 页）✅
- 首页主要组件 ≥48dp：SOS 200rpx、面板按钮 112rpx ✅

---

## Batch 14 · 真机联调 + Spark 链路自检 + docs 中文整理 ✅ 通过

### 14-1 真机访问后端（截图"网络异常"根因修复）
- **根因**：真机上 `localhost` 指手机自身；`utils/config.js` 缺少局域网地址导出，且与 `utils/request.js` 里手写的 resolveBaseUrl 产生 BASE_URL 重复声明（语法错误 → 请求模块整体失效 → 弹"网络异常"）。
- **修改**：utils/config.js（DEV_BASE_URL/LAN_BASE_URL + resolveBaseUrl 按平台切换 + 导出 BASE_URL，单一来源）；utils/request.js（删除本地重复实现，只消费 config.BASE_URL）。本机局域网 IP 实测 192.168.1.17。
- **验证**：node --check ✓；从局域网地址实际调用 /api/chat 通 ✓（源=local 降级链健康）。
- **状态**：通过。

### 14-2 Spark 链路自检 + 人设提示词 ✅ 通过
- **实现**：scripts/spark-check.js（+ npm run spark:check）：四环自检——.env 配置判定 / /api/chat 实际对话 / 医疗红线（头疼→引导就医，禁诊断/用药词）/ 降级链判定；routes/chat.js 人设提示词升级为三段式（身份与语气 / 对话方式 5 条 / 安全禁忌 3 条），支持 .env CHAT_SYSTEM_PROMPT 整段覆盖。
- **实测**：source=local（未配置密码的预期降级）；"我头疼" → 引导联系家人/就医，无越界词。
- **状态**：通过（spark 来源待填真实 APIPassword 后自动启用）。

### 14-3 docs 中文整理 ✅ 通过
- 根目录 8 份英文命名报告全部归档 docs/，统一简洁中文编号命名（00-导航 / 01-项目总结 / 02-审查报告 / 02~07 批次日志与报告 / 09-研发路线）；README-文档导航.md 重写为"用途 + 何时读 + 如何进行"表格。
- 状态：通过。

## 遗留（需人工）
- Windows 防火墙放行 3000 入站需管理员（netsh 被拒）；若真机仍连不上：防火墙 → 允许 Node.js / 入站规则放行 TCP 3000。

---

## Batch 14 补充 · 真机"网络异常"处置 ✅ 已处置（待真机确认）

- **根因链**：① 上一轮收尾时后端被停止（3000 无监听）→ 已重启，监听 0.0.0.0:3000，本机与局域网 IP 自测均 running；② 真机访问 localhost 指手机自身 → 已修复：config.js 按平台自动切换（devtools→localhost / 真机→http://192.168.1.17:3000）；③ Windows 防火墙可能拦截真机入站 → netsh 加规则需管理员（脚本内失败），已提供一键 bat。
- **提供文件**：根目录「启动后端.bat」（双击即起后端）、「放行防火墙3000端口(管理员运行).bat」（右键管理员运行，放行 TCP 3000 入站）。
- **状态**：服务端侧全部就绪；真机连通性待用户手机浏览器访问 http://192.168.1.17:3000/healthz 确认。
