# 桑榆智伴 · 交付前全量审查报告

- 审查日期：2026-08-30
- 审查范围：miniprogram/（15 页面 + custom-tab-bar + utils）、server/（Express + JSON 存储 + 脚本）、设计稿 sangyu-zhiban-draft/
- 开关状态：前端 `utils/config.js MOCK_MODE=true`、后端 `server/.env MOCK_MODE=true`（双开一致）
- 基线：后端 smoke 89 项 89/89 通过（本报告生成时实测）；演示数据已由 `npm run demo:reset` 铺设
- 方法：静态扫描（脚本）+ 路由/调用对照（脚本）+ 数据字段与 WXML 绑定人工核对 + 代码路径走查；**本批次未修改任何业务代码**（临时校验脚本已删除）

---

## 核心结论

> **当前达到「可答辩演示」状态。阻断级问题 0 个，严重级问题 0 个，一般级 4 个（均不阻断演示），建议级 6 个。**
> 「计划内未实现」7 项逐条确认见第 8 节，均未误报为缺陷。第 4 节 13 条演示链路代码路径全部 ✅。

---

## 1. 工程一致性审查

| 检查项 | 结果 | 说明 |
|---|---|---|
| 前端 MOCK_MODE | ✅ true | `miniprogram/utils/config.js:5` |
| 后端 MOCK_MODE | ✅ true | `server/.env:3` |
| appid 一致性 | ✅ | `project.config.json` 与 `server/.env` 均为 `wxf6a4508628dc3e41`（开发工具侧已完成真实 appid 绑定） |
| .env / sessions.json 入 .gitignore | ✅ | `.gitignore:4-5` |
| appsecret / session_key 泄漏扫描 | ✅ 无泄漏 | 仅 3 处命中均为安全用法：env 读取（routes/auth.js:20）、错误提示提及变量名（:24，不含值）、session_key 服务端存储（:46，写入 sessions.json，不下发） |
| 后端密钥相关日志 | ✅ 无 | routes/middleware/utils 无 secret/session_key/token 打印 |
| 小程序端 console.log / debugger | ✅ 无 | 0 处 |
| TODO 注释 | 5 处 | 均为计划内（云开发迁移 ×3、语音文件替换、voice 方案调研），见第 8 节 |
| JSON 可解析（含 .backup-* 除外） | ✅ | |
| 全部 .js node --check | ✅ | app.js + utils 6 个 + custom-tab-bar + 15 页 |
| WXML 标签闭合（含自闭合识别） | ✅ | 16 个文件 |
| WXSS 花括号平衡 | ✅ | 17 个文件 |
| 页面 WXSS 硬编码色值 | ✅ 零残留 | 上一轮 elder.wxss `#ffffff` 已令牌化为 `--color-danger-foreground` |

## 2. 接口契约审查

### 2.1 后端已挂载路由（32 条，全部有注释说明用途）

| 方法+路径 | 用途 | 前端调用 |
|---|---|---|
| POST /api/login | Mock 登录建档（演示模式） | login 页 |
| POST /api/updateRole | 更新角色 | login 页 |
| POST /api/auth/login | 真实登录（code→openid→token） | utils/auth.js（MOCK=false） |
| POST /api/auth/check | token 校验 | utils/auth.js / request 401 重登 |
| POST /api/relationship/create | 建立监护关系（幂等） | smoke/备用 |
| GET /api/relationship/list | 按身份查有效关系 | login / mine |
| POST /api/relationship/unbind | 解除关系（软删+级联设备） | mine |
| POST /api/bindDevice | 绑定设备（归属解析到老人） | bind-device |
| GET /api/device/list | 设备列表（status 实时计算） | bind-device / family / elder / mine |
| GET /api/device/status | 单设备状态+电量 | **前端未用（预留）** |
| POST /api/device/unbind | 解绑（软删+越权校验） | bind-device |
| GET /api/health | 健康记录查询（带 date） | family / health / health-elder |
| POST /api/health/mock | 写单条 Mock | health 页 |
| POST /api/health/mock/batch | 7 天演示数据 | health 页 |
| POST /api/health/report | 设备上报+规则引擎 | device-report |
| GET /api/health/stream | 设备数据流（调试） | **前端未用（调试用）** |
| GET /api/health/trend | 按日聚合趋势 | health / health-elder |
| POST /api/alert/create | 发起求助/代报 | elder / alert |
| GET /api/alert/list | 家属查告警（timeline 摘要） | alert |
| GET /api/alert/detail | 告警完整时间线 | alert |
| POST /api/alert/update | 状态流转+时间线 | alert |
| GET /api/alert/check-new | 轮询新告警 | family / polling |
| POST /api/appointment/create | 创建预约 | appointment |
| GET /api/appointment/list | 预约列表 | appointment |
| POST /api/report/generate | 规则引擎生成报告 | report |
| GET /api/report/list | 最近 5 份摘要 | report |
| GET /api/report/detail | 报告详情 | report |
| POST /api/invitation/generate | 老人生成邀请码 | invite |
| POST /api/invitation/bind | 家属输码绑定 | bind-relationship |
| GET /api/invitation/status | 查有效邀请码 | invite |
| GET / | 启动页（Dashboard） | 浏览器 |
| GET /healthz | 健康检查+计数 | 运维/脚本 |

### 2.2 契约对照结论

- ① 前端调用但后端不存在：**无** ✅（36 处调用全部命中）
- ② 后端存在但前端未用：5 条（device/status 预留、health/stream 调试、relationship/create smoke/备用、/ 与 /healthz 运维）——均为有意保留，非缺陷
- ③ 参数命名风险点（逐条）：

| 风险点 | 现状 | 风险级别 |
|---|---|---|
| health 记录归属字段：历史数据 `openid` vs 设备上报 `elderOpenid` | 路由层 `belongsTo()` 双字段兼容，已迁移存量数据 | 低（迁移云开发时需统一为 elderOpenid） |
| health/mock 与 mock/batch 写入字段 | 已统一写 `elderOpenid` | 已消除 |
| 设备上报 `timestamp`（可选）vs 存储双字段 `date`+`createdAt` | date 由服务端按本地时区计算，ISO 与本地日期并存 | 低 |
| `source` 值域：health 用 `device/mock`、alerts 用 `sos/rule_engine` | 两集合语义不同（数据来源 vs 告警来源），同名不同域 | 低（建议注释已标明） |
| bindDevice 入参 `openid`（会解析为 elderOpenid） vs unbind 入参 `operatorOpenid`（本人或授权家属） | 语义不同已注释 | 低 |
| alert/update `operator` 接受角色名或 openid | OPERATOR_TEXT 兼容两种写法 | 低 |

## 3. 数据字段与渲染一致性

- alerts.json 实际字段：`id, elderOpenid, reporterOpenid, source, type, severity, status, createdAt, updatedAt, timeline, currentHandler, resolvedNote, ruleId, message`（规则告警含 ruleId/message/evidence）——alert.wxml 绑定的 `item.elderOpenid/isRuleEngine(←source)/statusText(←status)/timeText/actions/displayTimeline` 全部有来源 ✅
- 状态机值域 `open / acknowledged / escalated / external_dispatched / resolved`：server（状态机表、normalize）、demo-reset、alert.js（STATUS_TEXT/ACTIONS/TIMELINE_TEXT）、alert.wxss（状态胶囊 class）共 10 处拼写一致 ✅
- 空值处理：health.wxml `{{item.heartRate === null ? '—' : …}}`、family 面板三指标 null → '—' + 灰色、health-elder `todayHr '--'`、report 无数据走 insufficient 报告 ✅（未发现会导致白屏的空值绑定）
- 设备字段 `battery/lastReportAt` null → 页面显示 "—"/“从未上报” ✅
- 预约 `status` 字段（demo 数据含 待处理/已完成）**WXML 未展示** → 归入计划内未实现（预约状态流转），见第 8 节
- 风险点：无字段名拼错；无缺失字段导致的绑定空串（除预约 status 未展示外）

## 4. 演示全链路代码路径审查（demo:reset 数据态，13 项）

| # | 链路 | 结果 | 代码路径证据 |
|---|---|---|---|
| 1 | 角色切换 | ✅ | login.wxml 两张 role-card `bindtap="onChooseRole"` + data-key → enterRole：MOCK 分支写 Mock openid → updateRole → 轮询钩子 → 家属无关系引导绑定 → switchTab |
| 2 | 老人长按 SOS | ✅ | elder.wxml 红条 + alert.wxml 圆钮均挂 `bind:touchstart/touchend/touchcancel="onSOSStart/onSOSEnd"`；≥1000ms → sendSOS → POST /api/alert/create；成功后 speak 反馈 |
| 3 | 设备上报→规则告警 | ✅ | device-report → POST /api/health/report → db 写入 → evaluateHealthRules → 命中去抖（5 分钟）→ createAlert(source=rule_engine)；demo 末条 125 与告警 A2 证据对应 |
| 4 | 家属端轮询推送 | ✅ | app.js onShow/登录钩子 → startFamilyPolling（15s 常量）→ check-new → vibrateLong + speak + setTabBarBadge(index 1) + showModal「立即查看」→ alert 页；onHide 停止 |
| 5 | 告警状态机+时间线 | ✅ | alert 页 ACTIONS 枚举 → onActionTap → showModal 填备注 → /api/alert/update → canTransition 校验 → timeline 追加（status/operator/note/at）→ resolvedNote；demo 4 条覆盖全状态 |
| 6 | 趋势图 | ✅ | health.wxml ec-canvas id=hrChart/bpChart/spo2Chart（spo2 条件渲染）+ selectComponent 对应 + onRangeChange 7/30 天重拉 trend |
| 7 | 老人端简要版 | ✅ | health-elder：/api/health 最新一条 + trend 7 天 → 大数字/状态点/sparkline（canvas id 保留）；阈值走共用 healthStatus |
| 8 | AI 报告 | ✅ | POST /api/report/generate（规则引擎，非大模型）→ 摘要/建议(level+evidenceRefs)/数据质量/disclaimer/evidence；demo 报告 1 份由接口生成 |
| 9 | 服务预约 | ✅ | 创建 + 列表（demo 2 条：社区医护待处理、志愿者已完成）；**状态流转为计划内未实现** |
| 10 | 设备生命周期 | ✅ | bind-device 列表（online/offline 按 lastReportAt 实时计算、电量 <20% 红、相对时间）+ 解绑二次确认 → 软删除 |
| 11 | 邀请码绑定 | ✅ | invite 生成 6 位码 + 10 分钟倒计时（status 接口恢复）→ bind-relationship 输码 → relationship/list 可查；demo 邀请码为空（现场生成） |
| 12 | 我的页 | ✅ | 关系列表（家属端带设备在线点）→ 撤销授权/解绑（二次确认）→ /api/relationship/unbind 软删 + 级联解绑设备；隐私政策 navigator；字体 actionSheet |
| 13 | 字体系统 | ✅ | fontClass 全 15 页根节点绑定 + app.wxss 三档 `--font-*` 变量（特大档主文字 44rpx）；Storage 持久化 |

## 5. 文案与合规审查

> CONTENT-REVIEW.md 不存在，以下为全量提取后的分组结果（按页面）。

- **login**：品牌名/slogan「温暖陪伴，安心守护」、角色卡「我是老人/我是家属」、隐私链接 ✅
- **elder**：问候语、SOS「一键求助/紧急时按下，联系家人」、「长辈模式已开启」、提醒卡、入口文案 ✅
- **alert**：状态枚举（待处理/已接单/等待社区响应/已派单/已处理）、时间线动作、弹窗「🚨 新的求助/预警」 ✅
- **health / health-elder**：指标名+单位、状态（正常/需要留意/异常）、空态「数据不足，暂无趋势」「今日暂无数据」 ✅
- **report**：免责声明全文「本内容根据已授权数据自动生成，仅供健康管理和康养参考，**不构成诊断、治疗或急救建议**。如有不适或紧急情况，请及时联系医疗机构或急救服务。」+「AI 生成标识（rule-engine-v1）+ 非诊断说明」 ✅ **免责三要素（非诊断/不构成治疗建议/及时就医或联系医疗机构）完整**
- **privacy**：收集范围/用途/存储/用户权利/联系方式五节，含「不构成诊断或治疗建议」 ✅
- **invite / bind-relationship / appointment / bind-device / mine / device-report**：引导与按钮文案均无越界表述 ✅

检查结果：

| 检查项 | 结果 |
|---|---|
| ① 占位符/TODO/debugger/console.log | TODO 5 处均为计划内注释（非用户可见）；debugger/console.log 0 处 ✅ |
| ② 假数据写死 | **4 处**（见问题表 G1/G2/G6）：elder 问候「上午好，张爷爷」+日期「8月30日·星期六」、family 状态卡「王大爷/78岁/最后同步·刚刚」、voice 演示气泡；均为设计稿还原的静态展示，非接口字段谎报 |
| ③ 医疗越界词 | ✅ 零命中——全部「诊断/治疗/急救」出现在否定/免责语境（report 免责声明、privacy、advice 仅「建议就医咨询/密切关注」级别）；无处方/用药/剂量/「立即拨打120」/确诊表述 |
| ④ 同一状态跨页文案一致 | ✅ 告警状态文案单一来源 alert.js STATUS_TEXT；健康状态单一来源 utils/healthStatus.js |
| ⑤ 免责声明完整性 | ✅ 报告页底部常驻 + 数据来源折叠区「非诊断」说明 + 报告接口返回固定 DISCLAIMER |

## 6. 适老化与设计还原度

- **还原度**：elder-home（Hero/提醒卡/SOS 红条/三宫格）✅、family-home（品牌栏/状态卡/健康面板/快捷操作/报告行）✅、role-select（琥珀满屏/角色卡）✅、elder-sos（220px 圆钮+脉冲+按住态→alert 老人分支）✅、voice-companion（静态结构）✅、health-data（品牌顶条+分段切换+面板）✅（echarts 图替代 HTML 静态图）、health-report（文档式头部+免责）✅
- **未还原项（已知）**：老人端底部 4 tab 含琥珀求助圆钮（涉及 app.json tabBar 重构，计划内）；family 首页紧急横幅改为常驻「预警中心」入口（原设计绑定具体告警数据）
- **适老化硬指标**：
  - 主文字 `--font-base` 36rpx（≥32rpx ✅），特大档 44rpx（≥44rpx ✅），全部页面根节点已接 fontClass
  - 主操作按钮 112rpx：login 角色卡/elder SOS(200rpx)/alert SOS(440rpx)/health 生成与演示/invite/bind-relationship/bind-device 确认/device-report/appointment/report/privacy/mine 退出 —— **11 个页面含 112rpx 主按钮 ✅**
  - 次级工具按钮（解绑 72rpx、绑定切换 72rpx、range 胶囊）低于 112rpx —— 设计权衡，标注为建议（非主操作路径）
  - 对比度：文字色/软底组合已按 ≥4.5:1 选型（warning 文字用深金 #8A6508）✅
  - SOS 防误触：双入口均为按住 1000ms + 松开确认 ✅

## 7. 问题清单总表

| 级别 | 模块 | 位置 | 现象 | 根因 | 修复建议 | 计划内未实现 |
|---|---|---|---|---|---|---|
| 一般 | 内容 | pages/elder/elder.wxml:11-12 | 问候「上午好，张爷爷」与日期「8月30日·星期六」写死，日期不随真实日期变化 | 设计稿静态还原；昵称/日期无数据源 | Batch 10：日期用 new Date() 计算，昵称接 users.nickname | 否 |
| 一般 | 内容 | pages/family/family.wxml:28-30 | 状态卡「王大爷/78岁/最后同步·刚刚」写死 | 同上（设计稿演示值，无真实姓名年龄采集） | 接 users.nickname + /api/health 最新 createdAt | 否 |
| 一般 | 数据展示 | server/demo-reset + routes/appointment.js | 预约 status（待处理/已完成）已落库但 WXML 不展示 | 预约状态流转属 Batch 7 计划内未做 | Batch 7+：列表项加状态标签 | 是 |
| 一般 | 工程清理 | pages/family/family.js | entries/onEntryTap 死代码 | 设计稿重构后入口改 navigator | Batch 10 清理（本批按约束保留） | 否 |
| 建议 | 接口 | device/status、health/stream、relationship/create、/、/healthz | 后端 5 条路由前端未调用 | 预留/调试/运维用途 | 保留；device/status 可供告警页快速展示 | 否 |
| 建议 | 内容 | pages/voice/voice.wxml | 演示气泡含「张爷爷」 | 静态视觉页 | 接真实昵称或随机话术 | 否 |
| 建议 | 安全 | server/data/sessions.json | session_key 记录只增不清 | 每次登录追加 | 增加保留期（如 7 天）或定期清理 | 否 |
| 建议 | 适老化 | unbind/bind-toggle/range 等次级按钮 | 高度 72–96rpx 低于 112rpx | 非主操作路径的设计权衡 | 保持现状；主操作已全部 ≥112rpx | 否 |
| 建议 | TODO | app.js/config.js/elder.js/report.js/voice.js | 5 处 TODO 注释 | 云开发迁移、语音文件、voice 方案均为计划内 | 按 Roadmap 执行 | 是 |
| 建议 | 阈值 | utils/healthStatus.js | 血氧阈值 90/95 为 MVP 约定 | 规则引擎暂未采集血氧 | 设备接入后与服务端规则统一 | 否 |

## 8. 「计划内未实现」逐条确认

| # | 事项 | 当前代码状态 | 是否影响答辩 |
|---|---|---|---|
| 1 | 预约状态流转与复诊合规导流 | ✅ 确认未做：仅 create/list；demo 预约带 status 字段但 UI 不展示 | 不影响（演示口径为“创建+列表”） |
| 2 | voice 真实语音通话（TRTC） | ✅ 确认未做：pages/voice 为静态视觉页，无事件绑定 | 不影响（入口与视觉已备） |
| 3 | Batch 10 数据级鉴权 | ✅ 确认未做：middleware/auth.js 仅 token 基础校验，MOCK 放行，req.user 已挂 | 不影响（Mock 演示） |
| 4 | 老人端底部 4 tab | ✅ 确认未还原：仍为 2-tab/1-tab，已琥珀化 | 不影响 |
| 5 | 全局请求失败降级页 | ✅ 确认未做：仅 toast + 页面内空态/占位 | 低影响 |
| 6 | 真实登录真机验证 | ✅ 确认未做：.env appsecret 为占位，MOCK_MODE=false 链路代码就绪但未真机验证；后端对未配置返回明确错误 | 不影响（演示走 MOCK） |
| 7 | 独立 pages/voice-companion | ✅ 确认未新建：视觉合并进 pages/voice（已注册） | 不影响 |

## 9. 回归与冒烟记录

- smoke 89 项：**89/89 通过**（本报告生成时实测一遍；两遍稳定性已在 Batch 8/9 验证）
- 本批次**零业务代码改动**（临时校验脚本创建后已删除）；演示数据保持 demo:reset 状态（告警 4 条、健康 8 条、报告 1、预约 2、设备在线）
- 附：REPORT 生成后如需复位演示数据：`cd server && npm run demo:reset`

---

## 审查签名

- 静态校验：JSON/JS/WXML/WXSS 全部通过（脚本复核）
- 接口契约：前端调用 100% 命中后端；5 条后端预留路由已标注
- 交互绑定：12 页 37 个事件方法 ↔ WXML 全对应
- 合规：医疗越界词零风险、免责声明完整、无敏感信息泄漏
